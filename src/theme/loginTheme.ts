import { StyleSheet } from 'react-native'

export const loginStyles = StyleSheet.create({
  title: {
    color: '#ffffff',
    fontSize: 30,
    fontWeight: 'bold',
    marginTop: 20
  },
  label: {
    color: '#ffffff',
    fontWeight: 'bold',
    marginTop: 25
  }
})
